services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "3010:3010"
    environment:
      NODE_ENV: production
      DATABASE_URL: file:/app/data/app.db
    volumes:
      - data:/app/data

volumes:
  data:
